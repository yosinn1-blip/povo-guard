{
  "name": "povo-guard",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "wrangler": "^3.78.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0",
    "@cloudflare/workers-types": "^4.0.0"
  }
}
